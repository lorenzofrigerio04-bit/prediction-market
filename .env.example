# Database (obbligatorio: PostgreSQL per dev e produzione)
# Locale: Docker Postgres o Neon free tier. Produzione: Neon / Vercel Postgres.
DATABASE_URL="postgresql://user:password@localhost:5432/prediction_market?schema=public"

# NextAuth (obbligatori)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# Cron: segreto per autorizzare le chiamate a /api/events/resolve-closed (cron job).
# Genera una stringa casuale sicura (es. openssl rand -base64 32).
CRON_SECRET=""

# Google OAuth: per abilitare "Accedi con Google" crea un progetto su
# https://console.cloud.google.com/apis/credentials e imposta redirect URI:
# http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
